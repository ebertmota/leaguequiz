import styled from 'styled-components';

export const Container = styled.header`
  background: rgba(0, 0, 0, 0.3);

  height: 70px;
  display: flex;
  align-items: center;
  padding: 20px;

  svg {
    display: flex;
    width: 60px;
    border-radius: 40px;
  }
`;
